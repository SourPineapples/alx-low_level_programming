#include <stdio.h>

/**
 * main - print all single digit of base 10 from 0
 * Return: Always 0
 */
int main(void)
{
	int numb;

	for (numb = 0; numb < 10; numb++)
		printf("%d", numb);

	printf("\n");

	return (0);
}
