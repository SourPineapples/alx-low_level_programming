#include "main.h"

/**
 * print_alphabet_x10 - printing by 10
 * Return: Always 0
 */

void print_alphabet_x10(void)
{
	int ten;
	char cla;

	for (ten = 0; ten <= 9; ten++);

	{
		for (cla = 'a'; cla <= 'z'; cla++);
		_putchar(cla);
		_putchar('\n');
	}
return (0);
}
