#include "main.h"

/**
 * _abs - computes the absolute value of an intege
 * @n: the int in questionn
 * Return: the value of the int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
