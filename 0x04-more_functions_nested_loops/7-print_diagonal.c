#include"main.h"
/**
 * print_diagonal -  draws a diagonal line on the terminal
 * @a: int
 */
void print_diagonal(int n)
{
	int z;

	if (a <= 0)
		_putchar('\n');
	else
	{
		for (z = 0; z < a; z++)
		{
			for (i = 0; i < x; i++)
				_putchar(' ');
			_putchar(92);
			_putchar('\n');
		}
	}
}
